1. 之前 user utility 的先验计算不考虑 input tokens，但是对于结果的 user utility 我们是考虑的

user_utility = reward - (input_tokens * input_tokens_price + output_tokens * output_tokens_price)

2. 之前的 input tokens 我们假设每一个模型都是一样的，但是其实是不一样的，会有轻微的差别